# SMTP for forgot-password OTP emails (copy to .env and fill in)
# Local/emulator: .env is loaded automatically.
# Production: set these as environment variables in Google Cloud Console (Functions → your function → Environment variables).

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASS=your-app-password
